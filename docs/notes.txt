AFTER PHASE 3:
    - refactor initialization Kiwi, structure more flat, each module factory return module container, with required
      objects initialized and pass container to next factory (can be decorator or chain)
    - refactor observability module, instead of direct implementation of each possible metrics class,
      implement container with several maps (counter, gauge, etc) which holds metrics by name.
      For adding metrics just call method "onAction(String metric name)" on one singleton instance of metrics
    - implement read buffer shrink
    - implement buffer for writing response (currently every time response is written new byte array stream is created)
      also with shrink
    - update logic on EXT command. Stop parsing any requests after parsed EXT. ConnectionContext and WriterProxy aware
      of EXT request id, and close connection only after EXT will be written to outputstream (unless some error didn't
      occure earlier which makes close connection before)
    - handle exception in accept loop
    - implement timeouts on requests, response, everywhere where thread might be blocked
    - test socket with different options (buffer size, latency, bandwidth)